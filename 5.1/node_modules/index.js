const express = require('express')
const dbConnect=require('')
const app = express()
const port = 3000


app.get('/', (req,res) => {
    res.send('Hello world!')
})

app.listen(port, () => {
    dbConnect();
    consol.log('Example app listining on port ${port}')
    })